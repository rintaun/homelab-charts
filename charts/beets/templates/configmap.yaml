---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "beets.fullname" . }}-config
  labels:
    {{- include "beets.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ toYaml .Values.beetsConfig | indent 4 }}
