---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "papra.fullname" . }}
  labels:
    {{- include "papra.labels" . | nindent 4 }}
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "papra.selectorLabels" . | nindent 4 }}
