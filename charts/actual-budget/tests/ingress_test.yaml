suite: actual-budget ingress tests
templates:
  - ingress.yaml
tests:
  - it: should not render Ingress by default
    asserts:
      - hasDocuments:
          count: 0

  - it: should render Ingress when enabled
    set:
      ingress.enabled: true
      ingress.host: actual.example.com
    asserts:
      - isKind:
          of: Ingress

  - it: should set the host correctly
    set:
      ingress.enabled: true
      ingress.host: actual.example.com
    asserts:
      - equal:
          path: spec.rules[0].host
          value: actual.example.com

  - it: should set TLS host correctly
    set:
      ingress.enabled: true
      ingress.host: actual.example.com
    asserts:
      - equal:
          path: spec.tls[0].hosts[0]
          value: actual.example.com

  - it: should set ingressClassName when specified
    set:
      ingress.enabled: true
      ingress.host: actual.example.com
      ingress.className: nginx
    asserts:
      - equal:
          path: spec.ingressClassName
          value: nginx
